[project]
name = "nammy"
version = "0.4.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.5.1",
    "tinygrad>=0.13.0",
]

[tool.uv.sources]
torch = { index = "pytorch-cpu" }

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[dependency-groups]
dev = [
    "pydantic>=2.13.4",
    "torch>=2.13.0",
]
# Only tools/build_msix.py needs this, and only on Windows.
packaging = [
    "pyinstaller>=6.11",
]
